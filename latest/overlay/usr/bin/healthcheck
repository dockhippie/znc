#!/usr/bin/env bash
set -eo pipefail
source /usr/bin/entrypoint

CHECK="$(curl -sL -w %{http_code} -o /dev/null ${ZNC_HEALTHCHECK_URL})"

if [[ "${CHECK}" == "${ZNC_HEALTHCHECK_CODE}" ]]; then
    exit 0
fi

exit 1
